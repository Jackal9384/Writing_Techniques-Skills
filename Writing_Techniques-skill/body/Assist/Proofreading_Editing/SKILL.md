---
name: 成品输出校验
description: >
  成品输出前的最终合规性校验与自动修正。在Core/NSFW等写作技能完成文本处理后、最终输出给用户前，调用此技能对标点符号合规性（依据CPS.json国标）和段落/字句结构合规性（依据Core规范体系）进行两阶段校验，不通过项自动修正至合格状态后输出最终成品。
---

# 成品输出校验

此技能是写作系统的**最终质量关卡**。任何写作技能（基础写作辅助、NSFW专项写作等）输出的成品文本，在交付用户之前必须经过此技能的合规性校验与自动修正。

## 核心定位

- **质量关卡 + 自动修正**：校验不过自动改到过为止，不回头找上游
- **双轨校验**：标点合规（CPS.json）+ 段落/字句合规（Core规范）
- **自闭环**：发现违规 → 自动修正 → 复检确认 → 输出
- **被动调度**：由 Core/NSFW 等写作技能在输出前主动调度

## 触发条件

- **自动触发**：Core 或 NSFW 技能在输出前自动调度此技能
- **手动触发**：用户直接要求"检验一下"、"检查标点"、"合规检查"等

## 关联文件

| 文件 | 位置 | 用途 |
|------|------|------|
| `CPS.json` | 本技能同目录 | 中文标点符号用法国家标准（GB/T 15834），标点合规性校验的唯一权威依据 |
| 基础写作辅助 SKILL.md | `body/Core/SKILL.md` | 段落节奏、间接描写等规范的校验基线 |

## 执行模式

由 Core/NSFW 调度时：跳过计划模式，直接按步骤执行。
用户直接触发时：进入计划模式（EnterPlanMode）。

---

## 执行步骤

### 步骤 0：接收待校验文本

接收上游技能传来的成品文本及相关元数据：

```
{
  "final_text": "{待校验的完整成品文本}",
  "source_skill": "{Core / NSFW / other}",
  "word_count": {字数},
  "chapter": "{当前章号/章名（如有）}",
  "content_type": "{叙事文本 / 对话文本 / 描写文本 / NSFW}",
  "exempted_norms": ["规范2", "规范2.2"]  // 从 Core 的 NSFW 返回数据包透传，无 NSFW 时为空数组 []
}
```

### 步骤 1：第一轮校验 — 标点符号合规性（CPS.json）

读取本技能同目录下的 `CPS.json`，以其中的标点符号定义、用法规则为校验依据，对成品文本进行全量标点扫描。**所有 E 级违规自动修正，所有 W 级违规在上下文可判定时自动修正**。

#### 1.1 扫描项目与自动修正规则

| 扫描项 | 依据 | 违规判定 | 自动修正动作 |
|--------|------|---------|------------|
| 句号 | §4 + §A.1 | 陈述句末尾漏句号；非陈述语气误用句号 | 补句号 / 改为对应语气标点 |
| 问号 | §4 + §A.2 | 非疑问句用问号；选择问句所有选项误加问号 | 改为句号 / 中间选项问号改逗号 |
| 叹号 | §4 | 无感叹语气滥用叹号；叠用超三个 | 改为句号 / 缩减至三个 |
| 逗号 | §4 + §A.3 | 长主语/句首状语后漏逗号；非提示性话语后误用冒号 | 补逗号 / 冒号改逗号 |
| 顿号 | §4 + §A.4 | 并列词语间漏顿号；概数间误用顿号；引号/书名号并列间多余顿号 | 补顿号 / 删顿号 / 删多余顿号 |
| 分号 | §4 + §A.5 | 非并列/非第一层分句间误用分号；含句号的分项仍用分号 | 改逗号 / 改句号 |
| 冒号 | §4 + §A.6 | 无停顿处用冒号；非提示性话语后误用 | 删冒号或改逗号 |
| 引号 | §4 + §A.7 + §B.2.1 | 内外标点位置错误；嵌套反序；段尾漏后引号 | 移标点至正确位置 / 换嵌套顺序 / 补后引号 |
| 破折号 | §4 + §B.1.7 + §B.2.3 | 断开分行；普通注释误用（应为括号） | 合并至同行 / 改为括号 |
| 省略号 | §4 + §A.9 + §B.2.4 | 与"等""等等"同时使用；超量叠用；断开分行 | 删省略号保留"等" / 缩减至标准 / 合并至同行 |
| 书名号 | §4 + §A.13 | 非作品名称套用；嵌套反序 | 改引号 / 换嵌套顺序 |
| 括号 | §4 + §A.8 + §B.2.2 | 句内括号未紧跟被注释词；行末多余标点 | 移至正确位置 / 删多余标点 |
| 连接号 | §4 + §A.11 | 数值范围误用短横；行首出现 | 改浪纹线 / 移至上行末 |
| 间隔号 | §4 + §A.12 | "和"连接标题中仍用间隔号 | 删间隔号 |
| 分隔号 | §4 + §A.14 | 前后多余点号 | 删多余点号 |
| **横排位置** | §5 | 行首出现句内点号；行末出现前引号/前括号；破折号/省略号分行断开 | 点号移上行末 / 符号移下行首 / 合并同行 |

#### 1.2 自动修正执行方式

对文本逐句扫描，发现违规后立即执行修正并记录修正日志：

```
🔧 标点自动修正日志：
  L3: "他推开门——" [破折号断开] → 已合并同行
  L7: "……等等" [省略号与"等"同用] → 已删省略号
  L12: "这是巧合还是有意安排？" [选择问句中位→逗号] → "？"改为"，"
  L15: "你知道吗？？？" [问号叠用超三个] → 缩减为"你知道吗？"
  L20: ""你好。"他说道。"好久不见。"" [引号嵌套反序] → 修正为'你好。'他说道。"好久不见。"
```

修正后立即对该句做复查确认修正成功。

#### 1.3 仅警告不修改的项（上下文无法判定时）

以下情况不做自动修正，仅在报告中标注供上游参考：

| 情形 | 原因 |
|------|------|
| 破折号与括号用途边界模糊 | 取决于作者意图（重要补充→破折号连读 / 普通注释→括号） |
| 分号/逗号在复杂多重复句中的选择 | 取决于作者对分句层次的判断 |
| 书名号与引号对"题"类短语的适用 | 取决于是否视为作品名 |

### 步骤 2：第二轮校验 — 段落/字句结构合规性（Core规范）

读取 `body/Core/SKILL.md` 中的写作规范，对成品文本进行结构性校验。**所有违规自动修正至合规状态**。

**校验前先读取步骤 0 输入数据包中的 `exempted_norms`**：如包含 `"规范2"` 则跳过 2.1.6（贴标签式性格描写），如包含 `"规范2.2"` 则跳过 2.1.7（大纲原文复刻）中与性格间接化相关的检查。其余规范照常过检。

#### 2.1 校验项与自动修正规则

##### 2.1.1 段落长度超标（规范 1）

**检验**：段落中完整自然句（以。！？… 收尾）超过 3 个 → 违规。

**自动修正**：在最佳拆分点将长段一分为二。
- 拆分点优先选择：句号之后 > 问号之后 > 叹号之后
- 如段落超过 5 个完整自然句 → 拆分为 2~3 段，每段 ≤3 句
- 拆分后检查：新段落的第一句是否仍能独立成意 → 如太短（≤5字）则并入下一段

```
修正前：
  她走进房间。窗帘紧拉着。光线昏暗。空气里浮着一层薄灰。她打开灯。然后看到了桌上的信。

修正后：
  她走进房间。窗帘紧拉着，光线昏暗，空气里浮着一层薄灰。
  
  她打开灯，然后看到了桌上的信。
```

##### 2.1.2 句末标点链接（规范 1）

**检验**：短句/超短句/长句之间出现"X{标点}Y{标点}"的链式结构 → 违规。

**自动修正路径**：
- 句号链接 → 合并为逗号，或拆段（优先合并）
- 问号链接 → **强制拆段**，每句独立成段（保留追问感）
- 叹号链接 → **强制拆段**，每句独立成段（保留冲击力）
- 省略号链接 → 强制拆段
- 混合标点链接（？！）→ 强制拆段

```
修正前："这是什么？这怎么可能呢？为什么会这样呢？"
修正后：
  "这是什么？
  
  这怎么可能呢？
  
  为什么会这样呢？"

修正前："他做到了！他真的做到了！"
修正后：
  "他做到了！
  
  他真的做到了！"
```

##### 2.1.3 超短句堆叠（规范 1）

**检验**：超短句（≤5字）成段时出现连续 ≥2 个句末标点 → 违规。

**自动修正**：将连续超短句改为逗号相连（同一意群）或各自换行（非同一意群）。

```
修正前："跑了。追。没追上。算了。"
修正后："跑了，追，没追上——算了。"

修正前："他停下。回头。皱眉。没说话。"
修正后：
  "他停下，
  
  回头，
  
  皱眉。
  
  没说话。"
```

##### 2.1.4 无必要短句断裂（规范 1）

**检验**：同一意群被句号无理切断（三问判定法：同一件事？无转折？每句不独立？全中 → 违规）。

**自动修正**：将句号改为逗号合并为一个完整句。

```
修正前："你累了。回去休息吧。"
修正后："你累了，回去休息吧。"

修正前："我知道。你不会来的。所以我没有等。"
修正后："我知道你不会来的——所以我没有等。"
```

##### 2.1.5 对话叙述被句号截断（规范 1 衍生）

**检验**：对话+叙述+对话的"三明治"结构中，中间叙述以句号结尾 → 违规。

**自动修正**：将句号改为逗号。

```
修正前："教宗大人。"丽丽没有收剑。"我只是想问一个问题。"
修正后："教宗大人。"丽丽没有收剑，"我只是想问一个问题。"
```

##### 2.1.6 贴标签式性格描写（规范 2.2）

**检验**：初登场文本中出现叙述者直接贴性格标签（"他是XX的人""她看起来XX"）→ 违规。

**自动修正**：将标签句改写为行为展现。如标签句无法确定对应的行为替代，标记 `[⚠️ 需人工: 性格标签→行为展现]` 向用户说明。

```
修正前："他是个莽撞冒失的人。"
修正后："他一头撞开门冲进来，才发现走错了教室——满屋子的人转头看他，他挠挠头，又撞上了门框。"
```

> 如上下文不足以推断合适的替代行为，标注 `[⚠️ 需人工]` 保留原文并提示用户。

##### 2.1.7 大纲原文复刻（规范 5）

**检验**：段落中出现与大纲（人物存档/设定文件）中原文高度重合的描述 → 违规。

**自动修正**：无法自动修正（需要创作级替换）。标记 `[⚠️ 降重提醒: 以下段落与大纲原文高度重合，建议换感官/换语境重写]` 并列出重合段落和对应大纲出处，由上游技能最终处理。

```
⚠️ 降重提醒：
  段落 2："B罩杯，底盘紧致，乳峰挺翘"
  → 与 大纲/人物/人物存档.md 中「林婉儿」条目原文高度重合
  → 建议：换触觉/互动角度重写（如"他解开时指尖擦过一处——刚好盈满"）
```

#### 2.2 自动修正执行流程

```
待校验文本
    │
    ▼
逐段扫描 ──→ 发现违规段 → 执行对应修正规则 → 写出修正后文本
    │
    ▼
全篇修正完成 → 生成修正日志
```

#### 2.3 输出结构修正报告

```
📋 段落/字句结构修正报告

🔍 扫描范围：{N} 段落，{N} 自然句

🔧 自动修正：
  • L5：段落长度超标（5个完整句）→ 拆分为2段
  • L8："这是什么？这怎么可能呢？"问号链接 → 逐句拆段
  • L12："跑了。追。没追上。"超短句堆叠 → 逗号合并
  • L18：对话叙述句号截断 → 改逗号
  • L22：贴标签式性格 → 已改写为行为展现
  • L30：大纲复刻 → 已标记 [⚠️ 降重提醒]（需人工确认）

✅ 结构修正完成（{N}处修正）
```

### 步骤 3：复检确认

两轮修正完成后，对修正后的文本再做一次快速扫描，确认所有违规已消除：

1. 标点：重新逐句检查，确认无新引入的标点错误
2. 结构：重新逐段检查，确认段落数、句末标点链接、超短句堆叠均已合规
3. 如发现修正引入的新问题 → 对该处再次修正后重检
4. 复检通过则进入步骤 4

### 步骤 4：输出最终成品

所有校验通过后，输出最终成品及校验摘要：

```
✅ 成品输出校验完成

📊 校验摘要：
  • 标点修正：{N} 处（句号X / 逗号X / 引号X / 破折号X / 省略号X / 其他X）
  • 结构修正：{N} 处（段落拆分X / 句号合并X / 拆段X / 性格改写X）
  ⚠️ 需人工确认：{N} 项（降重提醒X / 性格标签X）

━━━━━━━━━━━━━━━━━━━━
{修正后的成品全文}
━━━━━━━━━━━━━━━━━━━━
```

---

## 与上游技能的协作

```
Core/NSFW 输出成品文本
    │
    ▼
成品输出校验（此技能）
    │
    ├── 第一轮：CPS.json 标点扫描 ──→ 自动修正 ──→ 复检
    │
    ├── 第二轮：Core规范 结构扫描 ──→ 自动修正 ──→ 复检
    │
    └── 复检通过 → 输出最终成品（附带修正摘要）
```

**关键变更**：此技能自闭环。不再将违规文本退回上游——所有可自动修正的违规在此技能内部修正完毕，仅极少数需要创作级替换的项（如降重提醒）标记后提交用户确认。

## 注意事项

- 两轮校验独立运行，不可跳过任何一轮
- 标点修正仅改标点，不改词汇；结构修正可改段落/句子边界，不改叙事内容
- 修正后必须复检一次确认无新问题
- CPS.json 是本技能同目录下的标点权威数据源
- 如输入数据包中 `exempted_norms` 非空，结构校验（第二轮）跳过其中列出的规范项（由 Core 从 NSFW 返回数据包透传）；标点校验（第一轮）不受豁免影响
- 始终以 Markdown 格式输出校验报告和最终成品
- 所有修正动作均记录日志，确保可追溯
