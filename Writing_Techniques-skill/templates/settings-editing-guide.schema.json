{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "设定编辑板块引导数据结构校验",
  "description": "data/settings-editing-guide.json 的结构校验——各板块询问模板（sections）与正反例（examples）。",
  "version": "1.0",
  "type": "object",
  "required": ["schema_version", "sections", "examples"],
  "properties": {
    "schema_version": { "type": "string" },
    "description": { "type": "string" },
    "sections": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "required": ["title", "prompt"],
        "properties": {
          "title": { "type": "string" },
          "prompt": { "type": "string" }
        }
      }
    },
    "examples": {
      "type": "object",
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["wrong", "right"],
          "properties": {
            "wrong": { "type": "string" },
            "right": { "type": "string" }
          }
        }
      }
    }
  }
}
